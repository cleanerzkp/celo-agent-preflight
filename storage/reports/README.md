# Preflight Report Storage

Drop canonical `preflight.report.v0.1` JSON files in this directory to publish them
through the local ReadyList, `/api/reports`, `/api/agents`, and report pages.

The directory is intentionally empty until real scanner output exists. Demo agent
rows should not be used as grant evidence.
